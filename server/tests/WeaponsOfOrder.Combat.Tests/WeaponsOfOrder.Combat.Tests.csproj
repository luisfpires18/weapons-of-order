<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>WeaponsOfOrder.Combat.Tests</RootNamespace>
    <IsPackable>false</IsPackable>
    <OutputType>Exe</OutputType>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" />
    <PackageReference Include="xunit.runner.visualstudio" />
    <PackageReference Include="xunit.v3" />
  </ItemGroup>

  <!--
    The simulator only, with no host and no database. These tests are the reason the combat
    project has no other references: a rule that needs a database running to be checked is a
    rule that will be checked less often.
  -->
  <ItemGroup>
    <ProjectReference Include="..\..\src\WeaponsOfOrder.Combat\WeaponsOfOrder.Combat.csproj" />
  </ItemGroup>

</Project>
